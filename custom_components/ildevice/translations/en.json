{
  "config": {
    "step": {
      "user": {
        "title": "IL device",
        "description": "The devices that publish ildevice descriptors under one MQTT topic prefix, for example il/tuya or il/thinq. Add the integration again for another prefix.",
        "data": {
          "il_prefix": "Topic prefix",
          "offline_grace": "Seconds before a device is shown unavailable",
          "aliases": "Legacy entity ids (JSON)",
          "auto_add": "Add discovered devices automatically"
        },
        "data_description": {
          "aliases": "{aliases_example}, to keep existing unique ids.",
          "auto_add": "When off, each new device is offered first, and can be added or ignored."
        }
      },
      "discovery_confirm": {
        "title": "Add {name}?",
        "description": "{name} ({model}) is publishing an ildevice descriptor. Add it to Home Assistant?"
      }
    },
    "error": {
      "bad_aliases": "Not a JSON object of objects of strings.",
      "bad_prefix": "The topic prefix is required.",
      "duplicate_prefix": "Another hub already uses this topic prefix."
    },
    "abort": {
      "already_configured": "Already set up.",
      "device_added": "The device was added.",
      "no_hub": "Set up the integration first."
    },
    "flow_title": "{name}"
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "il_prefix": "Topic prefix",
          "offline_grace": "Seconds before a device is shown unavailable",
          "aliases": "Legacy entity ids (JSON)",
          "auto_add": "Add discovered devices automatically"
        },
        "data_description": {
          "auto_add": "When off, each new device is offered first, and can be added or ignored."
        }
      }
    },
    "error": {
      "bad_aliases": "Not a JSON object of objects of strings.",
      "bad_prefix": "The topic prefix is required.",
      "duplicate_prefix": "Another hub already uses this topic prefix."
    }
  }
}
