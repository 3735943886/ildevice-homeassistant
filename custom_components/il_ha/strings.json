{
  "config": {
    "step": {
      "user": {
        "title": "Intermediate Layer",
        "description": "Devices that publish Intermediate Layer descriptors over MQTT (for example rusthinq or rustuya).",
        "data": {
          "il_prefix": "Topic prefix",
          "offline_grace": "Seconds before a device is shown unavailable",
          "aliases": "Legacy entity ids (JSON)"
        },
        "data_description": {
          "aliases": "{\"<model or *>\": {\"<entity key>\": \"<key an earlier integration used>\"}}, to keep existing unique ids."
        }
      }
    },
    "error": { "bad_aliases": "Not a JSON object of objects of strings." },
    "abort": { "already_configured": "Already set up." }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "il_prefix": "Topic prefix",
          "offline_grace": "Seconds before a device is shown unavailable",
          "aliases": "Legacy entity ids (JSON)"
        }
      }
    },
    "error": { "bad_aliases": "Not a JSON object of objects of strings." }
  }
}
